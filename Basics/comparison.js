console.log(`2`>1);// here it will return true, as it will convert '2' to a number and then compare
console.log(null<0);// false
console.log(null==0);// false
console.log(null>=0);// true, as here null is coverted to 0 and then check is performed