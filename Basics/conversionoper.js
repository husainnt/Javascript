let score=22;
// what if we want to know the type?
console.log(typeof(score));// will print type of score
let ss='33';
console.log(typeof(ss));// string
let in_number=Number(ss)// 'Number(ss)' will convert the string to number
console.log(typeof(in_number)); // this will print number
// same conversion method can be folled by String, Boolean etc.
